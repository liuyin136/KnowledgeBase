# Local-First RAG Experimentation Platform v1.2 — Backend dependencies
# Python 3.12 (per infrastructure-environment-spec_v1.1.md §1)
#
# Install:  pip install -r requirements.txt
#
# Notes:
#  • torch is installed via the standard PyPI wheel (cu121 build). For a
#    CUDA-locked environment, override with the PyTorch index URL:
#      pip install torch --index-url https://download.pytorch.org/whl/cu121
#  • neo4j>=5.20 is required for `CREATE VECTOR INDEX ... OPTIONS` + fulltext
#    indexes (Neo4j 5.x vector support).

# Local-First RAG Experimentation Platform v1.3
# Python 3.13 + CUDA 13.3 相容版本

# backend/requirements.txt
fastapi
uvicorn[standard]
pydantic
pydantic-settings
neo4j
redis
rq
sentence-transformers
transformers
torch
torchvision
torchaudio
numpy
einops
python-multipart
httpx
tenacity
structlog
huggingface-hub
peft  